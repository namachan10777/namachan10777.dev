<blockquote>
  <slot />
</blockquote>

<style>
  blockquote {
    border-inline-start: var(--space-2xs) solid rgb(var(--gray-light));
    padding-inline: var(--space-md) 0;
    padding-block: 0;
    margin-block: 0 var(--space-md);
    margin-inline: 0;
    color: rgb(var(--gray));
    font-size: 1em;
  }
</style>
