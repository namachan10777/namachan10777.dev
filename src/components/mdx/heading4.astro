<h4>
  <slot />
</h4>

<style>
  @layer component {
    h4 {
      margin-block: 0.5rem;
      font-size: var(--font-size-lg);
      line-height: 1.25rem;
      font-weight: bold;
      color: var(--fg-strong);
    }
    h4::before {
      content: "#";
      font-size: var(--font-size-base);
      color: var(--fg-weak);
    }
  }
</style>
