<table>
  <slot />
</table>

<style>
  table {
    width: 100%;
    border-collapse: collapse;
    margin-block: 0 var(--space-md);
    margin-inline: 0;
  }

  table > :global(tr) > :global(th) {
    font-weight: var(--font-weight-semibold);
    background-color: rgb(var(--gray-light), var(--opacity-medium));
  }

  table > :global(tr) > :global(th),
  table > :global(tr) > :global(td) {
    border: var(--border-width-thin) solid rgb(var(--gray-light), 1);
    padding: var(--space-xs) var(--space-sm);
  }

  table > :global(tr:nth-child(3n)) {
    background-color: rgb(var(--gray-light), var(--opacity-low));
  }
</style>
