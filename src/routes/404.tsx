import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class="flex items-center justify-center gap-4">
      <h1 class="font-mono text-3xl font-bold">404</h1>
      <p class="font-mono text-xl ">NOT_FOUND</p>
    </div>
  );
});
