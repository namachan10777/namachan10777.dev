---
layout: ~/layouts/derive/Index.astro
---

import Event from "~/components/composite/Events.astro";
import PostPreviewList from "~/components/composite/PostPreviewList.astro";
import PublicationList from "~/components/composite/PublicationList.astro";
import NameAndIcon from "~/components/composite/TopProfile.astro";
import { links, events, pubs, posts } from "~/page-assets";

<NameAndIcon links={links} />

## Profile

A student at University of Tsukuba. Studying in distributed storage system
in HPC.

```rust
let region = "Tsukuba, 🇯🇵";
let contact = [
  format!("{}@{}", "admin", "namachan10777.dev"),
  format!("{}@{}", "mnakano", "hpcs.cs.tsukuba.ac.jp"),
];
```

## Timeline

<Event events={events} />

## Publications

<PublicationList pubs={pubs.slice(0, Math.min(3, pubs.length))} />

## [Posts](/post/page/1)

<PostPreviewList posts={posts.slice(0, Math.min(3, posts.length))} />

## [Notice](/notice)

このサイトについての3rd party license
