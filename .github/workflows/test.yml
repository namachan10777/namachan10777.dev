name: Test

on: [push]

jobs:
  test:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: test
      run: |
        npm install
        npm run test
    - name: build
      run: npm run dev
